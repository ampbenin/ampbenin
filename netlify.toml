[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["pdf-lib", "qrcode", "mongoose", "cloudinary"]

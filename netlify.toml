[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"  # le dossier généré par Astro avec l'adapter Netlify

[functions]
  directory = ".netlify/functions"  # ← fonctions SSR générées par Astro
  node_bundler = "esbuild"
